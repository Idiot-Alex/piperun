# PipeRun 服务端环境变量模板
# 复制此文件为 .env，按需修改后启动后端：
#   cp .env.example .env
#   node server/server.js

# ── 监听端口 ──────────────────────────────────────────────────────────────────
# PORT=3001

# ── CORS / WebSocket Origin 白名单 ────────────────────────────────────────────
# 逗号分隔，仅列出的 Origin 才允许访问。
# 本地开发默认值已包含 localhost:5173 和 localhost:3001，无需修改。
# 生产环境示例：
# ALLOWED_ORIGINS=https://piperun.example.com

# ── API Token（远程访问鉴权）─────────────────────────────────────────────────
# 未设置时，只允许 localhost / 127.0.0.1 访问 API。
# 设置后，非本地请求必须携带：
#   HTTP:      Authorization: Bearer <token>
#   WebSocket: 连接后发送鉴权消息（前端自动处理）
# 前端页面会自动弹出 Token 输入框，验证通过后存入 sessionStorage。
# 生成随机 Token 示例：
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# API_TOKEN=

# ── 反向代理模式 ──────────────────────────────────────────────────────────────
# 使用 nginx / Caddy 等反向代理部署时，Node.js 收到的 remoteAddress 始终是
# 代理的 127.0.0.1，本地自动信任会被绕过。
# 设置 TRUST_PROXY=true 可关闭本地自动信任，强制所有请求都需携带 API_TOKEN。
# 注意：必须同时设置 API_TOKEN，否则所有请求仍会被拒绝（返回 403）。
# TRUST_PROXY=true
